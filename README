# Phone Agent AI Backend

## Overview

This is a Node.js TypeScript project that integrates with Retell AI and OpenAI to create an AI-based phone agent, designed to replace human interaction in answering phone calls. The project leverages modern TypeScript features to ensure a robust and maintainable codebase.

## Installation

1. Clone the repository:

   ```bash
   git clone phone-agent-ai-backend

   ```

2. ```cd phone-agent-ai-backend

   ```

3. ```npm install

   ```

4. ```ngrok http 8080

   ```

5. Add the URL of ngrok with wss://[url of ngrok]/llm-websocket into your Retell AI agent custom LLM.
